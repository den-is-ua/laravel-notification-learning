#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

# --service-ports forwards VITE_PORT (default 5173) so `npm run dev` works from the host.
exec docker compose run --rm --service-ports node npm "$@"
